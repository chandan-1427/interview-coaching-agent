name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main
    paths:
      - 'Dockerfile.agent'
      - 'ai_interview_coach/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.version'
      - '.github/workflows/build-and-push.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Read version
        id: version
        run: |
          echo "version=$(cat .version | tr -d '\n')" >> $GITHUB_OUTPUT

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.agent
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:${{ steps.version.outputs.version }}
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:build-${{ github.run_number }}
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:buildcache,mode=max

      - name: Read agent config
        id: agent_config
        run: |
          echo "config=$(cat ai_interview_coach/agent_config.json | jq -c .)" >> $GITHUB_OUTPUT
          echo "version=$(cat .version | tr -d '\n')" >> $GITHUB_OUTPUT

          # Read all skills from the skills folder
          skills_array="[]"
          # Check if the skills directory exists before looping
          if [ -d "ai_interview_coach/skills" ]; then
            for skill_dir in ai_interview_coach/skills/*/; do
              if [ -f "${skill_dir}skill.yaml" ]; then
                skill_json=$(cat "${skill_dir}skill.yaml" | yq -o=json | jq -c .)
                skills_array=$(echo "$skills_array" | jq --argjson skill "$skill_json" '. += [$skill]')
              fi
            done
          fi
          echo "skills=$(echo $skills_array | jq -c .)" >> $GITHUB_OUTPUT

      - name: Register agent deployment
        id: register
        run: |
          # Create JSON payload file to avoid shell escaping issues
          cat > /tmp/payload.json <<'EOF'
          {
            "agent": {
              "name": "${{ fromJson(steps.agent_config.outputs.config).name }}",
              "author": "${{ fromJson(steps.agent_config.outputs.config).author }}",
              "description": "${{ fromJson(steps.agent_config.outputs.config).description }}",
              "version": "${{ steps.agent_config.outputs.version }}",
              "agent_trust": "${{ fromJson(steps.agent_config.outputs.config).agent_trust }}",
              "type": "research"
            },
            "skills": ${{ toJson(fromJson(steps.agent_config.outputs.skills)) }},
            "framework": {
              "name": "langchain",
              "version": "1.0.0"
            },
            "execution_cost": ${{ toJson(fromJson(steps.agent_config.outputs.config).execution_cost || fromJson('{}')) }},
            "deployment": {
              "url": "${{ fromJson(steps.agent_config.outputs.config).deployment.url }}",
              "docker_image": "${{ secrets.DOCKERHUB_USERNAME }}/ai-interview-coach:latest",
              "docker_digest": "${{ steps.docker_build.outputs.digest }}",
              "platforms": ["linux/amd64", "linux/arm64"]
            },
            "repository": {
              "url": "https://github.com/${{ github.repository }}",
              "commit_sha": "${{ github.sha }}",
              "build_number": "${{ github.run_number }}"
            },
            "metadata": {
              "documentation_url": "https://chandan-1427.github.io/ai-interview-coach/",
              "domain": "${{ fromJson(steps.agent_config.outputs.config).extra_metadata.domain || 'general' }}",
              "specialization": "${{ fromJson(steps.agent_config.outputs.config).extra_metadata.specialization || 'multi-purpose' }}",
              "last_updated": "${{ github.event.head_commit.timestamp }}"
            }
          }
          EOF

          response=$(curl -w "\n%{http_code}" --location 'https://bindus.getbindu.com/bindu-register' \
            --header 'accept: application/json' \
            --header 'X-Bindu-CI: true' \
            --header 'X-API-Token: ${{ secrets.BINDU_API_TOKEN }}' \
            --header 'Content-Type: application/json' \
            --data @/tmp/payload.json)

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "HTTP Status: $http_code"
          if [ "$http_code" != "200" ]; then
            echo "❌ Registration failed with HTTP $http_code"
            echo "Response: $body"
            exit 1
          fi

          agent_id=$(echo "$body" | jq -r '.agent_id')
          deployment_id=$(echo "$body" | jq -r '.id')
          echo "agent_id=$agent_id" >> $GITHUB_OUTPUT
          echo "deployment_id=$deployment_id" >> $GITHUB_OUTPUT
          echo "✅ Registration successful. Agent ID: $agent_id"

      - name: Wait for deployment to complete
        run: |
          agent_id=${{ steps.register.outputs.agent_id }}
          deployment_id=${{ steps.register.outputs.deployment_id }}
          max_attempts=60
          attempt=0

          while [ $attempt -lt $max_attempts ]; do
            status_response=$(curl -s -w "\n%{http_code}" "https://bindus.getbindu.com/agents/$agent_id/deployment/$deployment_id/status")
            http_code=$(echo "$status_response" | tail -n1)
            body=$(echo "$status_response" | sed '$d')

            if [ "$http_code" = "200" ]; then
              deployment_status=$(echo "$body" | jq -r '.status // .deployment_status // "unknown"')
              echo "⏳ Status: $deployment_status ($((attempt + 1))/$max_attempts)"

              if [ "$deployment_status" = "succeeded" ]; then
                echo "✅ Deployment succeeded!"
                echo "🚀 Agent URL: $(echo "$body" | jq -r '.deployment_url')"
                exit 0
              elif [ "$deployment_status" = "failed" ]; then
                echo "❌ Deployment failed: $(echo "$body" | jq -r '.deployment_error // .error')"
                exit 1
              fi
            fi
            sleep 10
            attempt=$((attempt + 1))
          done
          echo "❌ Deployment timed out"
          exit 1
